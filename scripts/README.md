# Scripts Directory

This directory contains build and deployment scripts.

## Planned Contents

```
scripts/
├── build.js           # Build script (concatenate, minify)
├── deploy.sh          # Deployment script
└── test.sh            # Test runner
```

## Current State

Deployment scripts are currently in the root directory:
- `deploy.ps1` (Windows PowerShell)
- `deploy.sh` (Bash)
- `auto-deploy.ps1` (Auto-deploy)

## Migration Notes

Scripts will be consolidated here during reorganization.
